<?xml version="1.0" encoding="UTF-8"?>
<view xmlns="http://asyncode.com/View" xmlns:db="http://asyncode.com/DataBase" xmlns:fs="http://asyncode.com/filesystem">
	<param name="offset" default="0"/>
	<param name="limit" default="10"/>
	<!--<define name="fileCreated" command="fs:create" fs:path="/views/{$viewName}.xml">-->
	<!--<define name="filesList" command="f:list" f:path="/">-->
	<!--<define name="list" command="fs:list" fs:path="/" fs:showDirs="false"/>	-->
	<define name="postslist"  command="db:query">
		select id,title,intro,date_trunc('minute',create_date) as create_date from blog.posts order by create_date DESC offset {$offset} limit {$limit}
	</define>
	<!--<define name="postspaginate" command="db:query">-->
	<!--	select DISTINCT (ROW_NUMBER() OVER (ORDER BY id)/10) as Row from blog.posts-->
	<!--</define>-->
</view>